import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'zzz.lalilulelo.www',
  appName: 'betme',
  webDir: 'dist',
  bundledWebRuntime: false,
  
};

export default config;
